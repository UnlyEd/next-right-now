/**
 * Represents a string containing a date value.
 *
 * @example 2020-10-25
 *
 * There is no difference with the TS "number" type, but it helps make obvious what the real data type is.
 */
export type DateDay = string;
