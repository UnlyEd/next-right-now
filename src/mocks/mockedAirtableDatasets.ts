import { AirtableDatasets } from '../types/data/AirtableDatasets';
import mockedRawAirtableDataset from './mockedRawAirtableDataset';

export default {
  raw: mockedRawAirtableDataset,
  sanitized: {},
} as AirtableDatasets;
